import React from 'react'

function Button({className, onClick, btnText}) {
  return (
    <>
      <button />
      <button className = {className} onClick = {onClick}>{btnText}</button>
    </>
  )
}

export default Button
